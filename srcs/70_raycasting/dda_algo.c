#include "cub3D.h"

// take s_ray with start point and direction defined
void	ft_compute_ray_hit_point(t_data *data, t_ray *ray)
{
	//float	l_dx;
	//float	l_dy;
	//float	m;

	//m = tan(ray->angle_rad);

	//sqrt(pow() )

}
